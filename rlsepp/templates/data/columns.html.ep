%= javascript begin

function initHook() {
  var table = $('#resultSet').DataTable();
}

function columns() {
  var columns = []
% foreach my $header (@$headers) {
  columns.push({ data: "<%= $header %>"});
% }
  return columns;
}

%= end
